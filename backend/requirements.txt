# FastAPI 核心依赖
fastapi>=0.127.0
uvicorn[standard]>=0.40.0
python-multipart>=0.0.9

# 数据库
sqlalchemy>=2.0.0

# AI 模型
openai-whisper>=20231117

# 测试依赖
pytest>=9.0.0
httpx>=0.28.0
pytest-asyncio>=0.24.0
pytest-html>=4.0.0

# 环境变量管理
python-dotenv>=1.0.0

# 系统监控
psutil>=5.9.0

# 音频处理
# 注意：需要系统安装 FFmpeg（ffprobe 用于获取音频时长）
# Windows: 下载 FFmpeg 并添加到 PATH
# macOS: brew install ffmpeg
# Linux: apt-get install ffmpeg
